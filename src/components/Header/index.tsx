import { HeaderContainer } from './styles';
import Logo from '../../assets/Logo.svg';

const Header = () => {
  return (
    <HeaderContainer>
      <Logo />
    </HeaderContainer>
  );
};

export default Header;
